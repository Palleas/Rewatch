platform :ios, '9.0'

use_frameworks!

target 'Rewatch' do
  pod 'p2.OAuth2', '~> 2.0'
end

plugin 'cocoapods-keys', {
  :project => "Rewatch",
  :keys => ["BetaseriesAPIKey", "BetaseriesAPISecret"]}
